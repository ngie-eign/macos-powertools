cmake_minimum_required(VERSION 4.0)

set(BINDIR $ENV{HOME}/.local/bin/)
set(BINMODE OWNER_EXECUTE OWNER_READ)

install(
  DIRECTORY .
  DESTINATION ${BINDIR}
  FILES_MATCHING
  REGEX "[cC]?[mM]ake.*" EXCLUDE
  PATTERN "*.sh"
  PERMISSIONS ${BINMODE}
  )
